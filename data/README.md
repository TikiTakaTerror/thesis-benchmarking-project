# Local Data Storage

This directory stores dataset files that should not live inside `src/`.

Layout:
- `raw/`: original downloaded files
- `processed/`: prepared datasets in the local contract used by adapters

Phase 2 adds the folder contract and MNLogic validation flow.

