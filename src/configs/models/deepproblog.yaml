family: deepproblog

shared_encoder:
  name: small_cnn
  pretrained: false
  input_channels: 3
  input_size: [64, 64]
  conv_channels: [32, 64]
  feature_dim: 128
  dropout: 0.1

logic_program:
  file: null
  neural_predicates: []

training_defaults:
  batch_size: null
  learning_rate: null
  max_epochs: null

notes:
  status: Shared encoder defaults added in Phase 3. Integration is planned for Phase 7.
