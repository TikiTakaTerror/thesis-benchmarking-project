name: label_only

signals:
  labels: true
  concepts: false
  logic_constraints: false

concept_supervision:
  fraction: 0.0

notes:
  status: Active in R5. This means task-label supervision only, with no concept supervision and no extra logic-constraint loss.
