name: rsbench

paths:
  root_dir: external/rsbench-code

dataset_support:
  mnlogic: planned
  kand-logic: planned

suite_metrics:
  - accuracy
  - macro_f1
  - id_performance
  - ood_performance

notes:
  status: Placeholder only. The adapter will be added in later phases.

